<h2><%= t('view.sign_in.title') %></h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email, t('view.sign_in.login') %><br />
  <%= f.email_field :email %></div>

  <div><%= f.label :password, t('view.sign_in.password') %><br />
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :remember_me, t('view.sign_in.remember') %></div>
  <% end -%>
	
  <br />
  <div><%= f.submit t('view.sign_in.sign_in') %></div>
<% end %>

<%= render "links" %>